# 修改要求执行清单与当前状态

**需求文档**：`cpa2sub2api修改要求.docx`（9 条，含 11 张附图）
**最近核实**：2026-09-19

本文件是**逐条核实的结论**，不是计划。每条都标注了修改前后的真实状态与证据位置。
状态含义：✅ 已完成并验证 ｜ ⚠ 部分完成 ｜ ❌ 未完成 ｜ ⛔ 已推翻（原结论不成立）

> **2026-09-19 增补**：本轮以 8 个只读子代理分区勘查全库 + **本机实跑容器端到端**的方式复核，
> 又定位并修掉 6 个此前未被发现的实质缺陷（含 2 个会让整条链路静默失效的）。
> 详见文末「五、2026-09-19 实测复查：新发现并已修的问题」。本文件中与之冲突的旧结论以下一节为准。

---

## 一、功能要求

### 第 1 条：CPA 间接调用 503 排查 ✅ 已定位根因

**现象**：同一上游直连（填进 cc-switch）正常，经 CPA/sub2api 中转后报 503。

**核实结论**：不是一个问题，是三类不同的成因，需要分别识别：

| 现象 | 真实成因 | 判据 |
|---|---|---|
| `503 No available accounts` + 分组权限提示 | 该上游**只接受特定客户端**（如只允许 Claude Code），请求头没有伪装到位 | 错误文案里直接写了 `this group only allows ...` |
| `400 invalid codex request` | 凭据的**自定义模型列表**含上游不存在的模型名，或协议模式（responses / chat_completions）不匹配 | 错误里带 `request id`，属于上游拒答 |
| 低优先级站点从不被尝试 | 凭据级错误被网关判为**非重试**，请求死锁在同一个已耗尽账号上 | 日志里 `sticky_honored=true`、`non-retryable`、`pool_mode_error_skipped` |

第三类是真正属于本工具能改善的，见第 4 条⑴。

**证据**：`logs.txt`（2026-09-12 23:46–23:55，15 次请求）
**文档**：`tutorial.html` 第 8.2 / 8.3 / 8.4 节

---

### 第 2 条⑴：同域名同优先级 + 全参数检测填充 ⚠ 部分完成

| 子项 | 状态 | 说明 |
|---|---|---|
| 同域名多 KEY 同优先级 | ✅ | `_host_tier_map` 组内取最高值归一化，并有 `check_priority_drift` 出声告警 |
| 参数全部检测填写 | ✅ | 14 项参数迁移，含 credentials / priority / concurrency / 请求头 / 冷却规则 / 模型白名单 |
| `weight` 迁移 | ⚠ | CPA 的 weight 只写进 notes，**不发给 sub2api**（sub2api 无对应字段，负载分配由 `load_factor` 承担） |
| `cloak` 迁移 | ⚠ | 写入 `extra`，但 sub2api 网关层未实现消费逻辑，写入后不生效（已在 notes 说明） |
| TLS 指纹检测 | ⚠ | 只做字段搬运（`enable_tls_fingerprint`）；**"检测上游是否需要 TLS 代理"未实现**，模板需人工配置 |

---

### 第 2 条⑵：模型"就高原则" ✅ 已重写

**修改前的问题**（均实测复现）：
- 代际门槛硬编码（`claude-\w+-5`、`^gpt-6`），上游发新一代时会把新模型当旧代全砍；
- `gpt-4o` / `gpt-4o-mini` 被同族的 `gpt-4.1` 挤掉，整族静默消失（白名单是白名单语义，等于账号对这些模型退出调度）；
- gemini 找不到 `-pro` 时**静默**放行全部 flash，违反"必须带 pro"；
- 就高逻辑在仓库里有**三份不同步的拷贝**（`model_selection.py`、`tool.py` 死代码、探活计划）；
- 从上游源码反推的门槛 `MODEL_SERIES` 喂给了**从不执行**的死函数，告警会响、行为不变；
- `out/对照表.md` 显示的是**过滤前**的模型数，遮住了上面的静默裁剪。

**修改后**：
- `model_selection.py` 成为**唯一权威实现**，死代码与重复拷贝已删除；
- 代际门槛动态（从上游源码反推；反推失败则用"列表里观测到的最高代"），上游发 7 代时自动跟随；
- **codex 与 openai 按来源段分流**（新增 `source_section` 字段贯通 `collect` → 白名单写入 → 对照表）；
- 回退不再静默：任何回退都通过 `fallback_notes` 报出来；
- 对照表口径与线上白名单统一。

**验证**：`tests/test_fixes.py` 的 D1–D16、E1–E3、F1–F3

---

### 第 2 条⑶：智能优先级设计 ⚠ 部分完成

**修改前的问题**：
- 两套健康分口径冲突（一套用 `total`、0–100 分；一套用 `known`、0–1 分），权重都符合要求的 60/40，但分母与量纲不一致；
- `remap_priority_smart` 读账号列表时**既没传分页、又取错字段**（`items` vs `data.items`），静默拿到空集 → 所有域名健康分为 0 → 智能优先级从未真正生效；
- 首次导入（sub2api 无账号）时必须**手工再跑一遍**才能应用智能优先级。

**修改后**：
- 分页与字段取值修正，读到空集时明确回退并出声；
- `health_rerank_priority` 改为**全局统一定序**（桶号全局唯一）；
- **新增第二阶段自动重排**：导入完成后立刻用新产生的真实状态再过一遍，不需要手工跑第二遍。

**分母口径已定（2026-09-16 拍板）**：用"已导入的 KEY 数"（`known`），保持现状不改。

需求文档只写"可调度比例×60% + 活跃比例×40%"，**没有定义分母**。两种取法实测差异：

同一域名 5 个 KEY，其中 3 个已导入（2 个可调度、2 个活跃）、2 个是新增的：

| 分母 | 健康分 | `load_factor` |
|---|---|---|
| `known`（已导入 3 个） | **0.667** | 4 |
| `total`（全部 5 个） | 0.400 | 2 |

用 `total` 的后果是**每加一个新 KEY，该域名的健康分就被稀释一次** ——
上例中扩容 2 个 KEY 就让老域名的 `load_factor` 从 4 掉到 2，流量份额腰斩，
而它的实际表现一点没变。这与"原有 KEY 和上游不能丢失/不能被降级"直接冲突。

整个域名都是新增（`known=0`）时给中位分 0.5：不因"查无记录"被打到队尾，
也不凭空排到已验证的健康站前面。

**注**：仓库里有两套健康分实现（`tool.py::health_rerank_priority` 用 `known`、
`new_remap_priority.py` 用 `total`），这是**项目原有**结构。执行顺序是
`build_plan`(第 2100 行) 先算、`do_import`(第 2545 行) 后算并覆盖，
所以**最终生效的是 `known` 那套**。前者的独立价值是让空跑预览（菜单 2）
也能看到智能桶号，因此保留不删。

---

### 第 2 条⑷：反测活 ✅ 已增强

**修改前**：15 条英文 prompt 池；cron 固定 `*/30`，**所有账号在同一分钟一起探活**。

**修改后**：
- prompt 池扩到中英双语、30 条，覆盖多种日常话题；
- **每个账号派生一个稳定的分钟偏移**（`_cron_with_offset`），把"全体同时探活"这个最强的机器特征打散。
  实测 100 个账号会散到 29/30 种可能的探活时刻。

**依据**：探活系统识别机器行为时，**调用模式的规律性**（间隔方差为 0、全体同时）
比单条 prompt 的措辞权重更高；真实用户的请求时刻是分散的。

**验证**：`tests/test_fixes.py` 的 I1–I7

---

### 第 3 条：自动同步上游、严禁硬编码 ✅ 已收敛

**修改前**：存在**两条并行且互不相干的同步链路**
- (a) `tool.py:sync_constants` + `extract.py` —— 读本地上游源码，是真正在跑的那条；
- (b) `sync_upstream.py` —— 走 GitHub API 抓另一批文件，产出 `upstream_constants.json`。
  **无任何调用方、无消费者、CI 编译一个不存在的 `sync_upstream_v6.py`、抓的 5 个文件里没有一个含模型名、仓库名还是 `aurora-pro/sub2api`。**

**修改后**：删除 (b) 整条僵尸链路（含 `upstream_constants.json`），统一收敛到 (a)；
并把 `MODEL_SERIES` 真正接入生效路径（原先喂给死代码），代际门槛现在会跟随上游前移。

---

### 第 4 条⑴：120s 降级 / 524 容灾 ⚠ 已定位真实机制

**⛔ 推翻的原结论**：旧文档声称"降级时间从 120 秒优化至 5 秒，已实现"。
**核实结果：代码里没有任何降级延迟的实现或测量**，也没有它给的验证命令所指向的
`out/调度优化报告.md` 这个文件。那是一句没有依据的声明。相关文档已更正。

**真实机制**（有日志实锤）：120 秒是 **Cloudflare 的回源超时**，属于 CF 边缘的计时器，
源站侧（nginx 的 `proxy_read_timeout`）无法延长。真正要解决的是
"请求为什么会一直撞在同一个坏上游上"。日志显示完整链条：

```
请求进来 → 选号命中某个已耗尽的账号（sticky 粘住）
        → 上游返回 402 Budget pool quota has been exhausted
        → 网关判为 non-retryable，不换号（pool_mode_error_skipped）
        → 15 次请求全部打到同一个账号
        → 低优先级站点一次都没被尝试
        → 客户端侧表现为 503 / 524
```

**已修**：
- **域名级冷却规则**：402「预算池耗尽」是**整个上游**层面的状态
  （同一站的所有 KEY 共用同一个池子），不是单个 KEY 的毛病。
  为其下发更长（180 分钟）、关键词更特异的冷却规则，避免在同一池子里逐个 KEY 空撞；
- **桶号全局唯一**：保证"整桶挂掉才降级"真的能降级（原来跨渠道会撞桶）；
- **同域名同桶**：桶内 KEY 互为备份，一个被冷却后请求轮到同桶其它 KEY。

**验证**：`tests/test_fixes.py` 的 H1–H5、J1–J5

---

### 第 4 条⑶：同步已有数据、去重 ✅ 已完成

- 去重键是账号名（含 `sha1(api_key|base_url)` 前 8 位的内容指纹），与位置无关，删改 config 后仍能正确识别同一凭据；
- `diff_account_fields` 只发真正变化的字段（PUT 是整体替换语义，所以任一子项变化就整包重发 `credentials`）；
- 读不到账号列表时**中止**而不是"当作首次导入"，避免建出重复账号；
- **不覆盖** `api_key`（列表接口脱敏成掩码，无法比对）与 `status/schedulable` 等运行时状态。

---

### 第 4 条⑷：mihomo 链接失败 ✅ 已修复

**修改前**（本仓库那套实现是残缺的）：
- `mihomo/config.yaml.example` 的 `allow-lan: false` → 跨容器连接会被 mihomo 的准入策略拒掉；
- 示例里硬编码 5 个 `server.example.com` 假节点，且**没有 `proxy-providers` 订阅段** → 照抄永远连不通；
- compose 的 healthcheck 用 `curl`，但 `metacubex/mihomo` 镜像里**没有 curl** → 该检查恒定失败；
- 而 `cpa2sub2api` 又 `depends_on: service_healthy` → **两个容器互相锁死**，本工具永远不启动；
- bind mount 整个宿主 `mihomo/` 目录 → 违背"VPS 只拉镜像、宿主不预置文件"的部署前提。

**修改后**（复用 upstream-importer 的实现）：
- 补齐 `mihomo-manager/`（自举脚本、订阅更新脚本、模板、healthcheck.py）；
- 改为 **init 容器 + 命名卷**：`mihomo-init` 从镜像内模板物化配置，跑完即退，`mihomo` 再启动；
- healthcheck 改用 `python3 healthcheck.py`（出口探活 + 失败自动降级 DIRECT）；
- 依赖关系降为 `service_started`，不再因"代理出不去"而阻塞本工具启动；
- `.dockerignore` 停止整目录排除 `mihomo/`（模板必须进镜像）。

**另外修掉的一处**：`FALLBACK_PROXY` 环境变量是**死配置** ——
代理兜底函数只读 `设置.json`，而容器部署全靠环境变量下发，
导致**容器里代理兜底完全不生效**。这是"配了 mihomo 却没用上"的直接原因。

**验证**：`mihomo-manager/README.md` 的四步验证法（含从 sub2api 容器内部验证）

---

### 第 5 条：按域名分桶 + priority 定序 ⛔ 已修（原实现有实质缺陷）

**归集键**：`(渠道, 域名)`，组内取最高 CPA priority 作序位、全局 dense rank —— 这些原先就做对了。

**但发现了实质缺陷**：桶号的**防碰撞微调只写在同渠道循环内部**，
只能保证"同渠道内不撞"。端到端实测：三个不同渠道的三个不同域名，
在 CPA priority 相同时**全部拿到桶号 10**，被 sub2api 当成同一桶轮循，
跨域名降级容灾事实上失效。

**修改后**：改为**全局唯一定序**（按序位降序 → 渠道名 → 域名字典序编号），
桶号全局唯一，且同一份配置多次运行结果稳定。

**验证**：`tests/test_fixes.py` 的 J1–J5

---

### 第 6 条：正向提升、并发提速 ⚠ 部分完成

**已有**：建号 / 探活 / 定价三条链路都用线程池。

**已修**：
- `test_plan_workers` 缺上限钳制（其余三处都有 `min(...,8)`），配成 100 会真起 100 线程；
- `health_rerank_priority` 的内层循环是 O(记录数 × 域名数)，已提到循环外；
- `build_plan` 里 `_host_tier_map` 在 for 循环内重复整表重算，已提前；

**仍为串行**（未改动，规模下影响有限）：`ensure_groups`、`ensure_proxies`、
`reclaim_schedulable` 的写回、上游源码抓取。

---

### 第 7 条：脱敏与安全提交 ✅ 已完成（历史已重建）

**发现并处理**：
- ⛔ 已跟踪的 `修改要求.docx` 内含 **10 张截图**，其中一张是**明文 API Key + 真实上游域名**的界面截图。
  二进制文件无法靠文本搜索审出来，已删除并加入 `.gitignore`；
- 多份 markdown 含真实上游域名，已脱敏为占位符；
- `graphify-out/`（本机代码图谱，含全量路径与符号表）已忽略；
- **git 历史已重建**：旧历史的那一个提交里就含上述 docx 的 blob，
  仓库从未推送过，所以直接重建历史、以干净状态重新开始。

- **镜像名改为变量驱动**：不再在仓库里写死任何 Docker Hub 账号名，
  由 `.env` 的 `DOCKERHUB_USERNAME` / `DOCKERHUB_IMAGE` 与 GitHub 仓库
  变量共同决定（两处同名，一处配置两边一致）。

---

### 第 8 条：VPS 拉远程镜像部署 ⛔ 已修（链路原本是断的）

**发现**：
- 三个 GitHub Actions workflow 全部只在 `main` 分支触发，而**仓库的默认分支是 `master`**
  → 推送后 CI 一次都不会跑，**镜像永远不会被构建**。而部署方式正是"VPS 拉远程镜像"，
  这条链路就断在这里，且界面上看不出任何报错，只表现为"改了代码但 VPS 拉到的还是旧镜像"；
- 两个 workflow 做同一件事却用了**不同的 secret 名**（`DOCKER_PASSWORD` vs `DOCKER_HUB_TOKEN`），
  其中至少一个必然因读不到 secret 而登录失败；
- `python-check.yml` 编译一个**不存在的** `sync_upstream_v6.py`，CI 必然失败。

**修改后**：
- 分支改为 `master`（同时保留 `main`，便于将来切换）；
- 删除重复的 workflow，只保留一个；
- secret 名统一为 `DOCKER_HUB_TOKEN`（**仓库里只需配置这一个**）；
- 修掉不存在的文件引用，并把回归测试接进 CI。

---

### 第 8 条补充：VPS 侧三个执行文件的配置

需求点名要求改这三个文件：
`fsdownload/.env`、`fsdownload/docker-compose.yml`、`fsdownload/nginx.conf`。

**核实结果（2026-09-15）**：这三个文件里**本项目相关配置原本一处都没有** ——
`docker-compose.yml` 的 31 个服务里没有 `cpa2sub2api`，`.env` 里也没有任何本项目变量。

**已补**：

| 文件 | 改动 |
|---|---|
| `.env` | 追加本项目全量配置：`DOCKERHUB_USERNAME` / `DOCKERHUB_IMAGE`、sub2api 与 CPA 连接、代理、并发、策略开关、HTTP 行为 |
| `docker-compose.yml` | 新增 `cpa2sub2api` 服务（`restart: "no"`、`pull_policy: always`、接入 `sub2api-network` 与 `default` 双网络）+ `cpa2sub2api-out` 命名卷 |
| `nginx.conf` | **不需要改** —— 本项目是一次性运维工具，不占端口、不进请求路径 |

**一个必须注意的点**：VPS 栈里**已经有 mihomo**（由 `swhesong/cpa-upstream-importer`
镜像的 `mihomo-init` 自举）。所以本项目接入时**只加自己的服务**，绝不能再定义
第二份 mihomo —— 否则服务名与 container_name 冲突。

验证：`docker compose config` 解析通过，镜像名正确展开为
`${DOCKERHUB_USERNAME}/${DOCKERHUB_IMAGE}:latest`。

---

### 第 9 条：配合排查 ✅ 已提供

`tutorial.html` 第八章给出可自助完成的排查步骤（8.1–8.8），
覆盖 mihomo "链接失败"、低优先级不降级、503、400、524、空 SSE、1010 等全部已知故障。

---

## 二、验收状态

### 功能
- ✅ 模型"就高原则"生效且可验证（56 项回归测试）
- ✅ 同域名同桶、桶号全局唯一
- ✅ 冷启动自动两阶段重排
- ✅ 并发导入
- ⚠ 503 三类成因已分类定位；第三类已修，前两类属上游/CPA 侧配置
- ❌ TLS 指纹"是否需要"的自动检测（未实现，需人工配置模板）

### 部署
- ✅ docker compose 一键启动（`--profile mihomo` 带代理）
- ✅ mihomo 自举（init 容器 + 命名卷，宿主无需预置文件）
- ✅ GitHub Actions 已验证：推送后 CI 跑通（3 个 Python 版本测试 + 多架构构建），
  镜像已推到 Docker Hub（`swhesong/cpa2sub2api`）与 GHCR，`linux/amd64` + `linux/arm64`
- ⚠ VPS 生产环境运行稳定性需实战观察

### 安全
- ✅ 无硬编码真实域名 / 密钥
- ✅ 敏感文件（含明文密钥截图）已删除
- ✅ `.gitignore` 覆盖 `.env` / `设置.json` / `config.yaml` / 订阅清单 / `*.docx` / `*.mhtml` / `graphify-out/`
- ✅ git 历史已重建，不含任何旧 blob

### 文档
- ✅ `tutorial.html` 完整教程（分级标题、配色、17 张自绘示意图、30 个一键复制按钮、逐项配置说明；`config.yaml` 逐字段含义见 4.5 节，VPS 验收清单见第十一章）
- ✅ `README.md` 反映真实能力
- ✅ `mihomo-README.md` / `mihomo-manager/README.md` 与实现一致
- ✅ 本文件：逐条核实的真实状态

---

## 三、密钥排障记录（2026-09-15/16，非本项目代码问题）

部署验证过程中遇到一次 CPAMP 面板登不上的排障，过程曲折，结论已写入教程 8.9–8.13。
**这不是本项目的代码缺陷**，但属于同一套部署的运维知识，记下来避免重踩。

### 结论：改错了对象

CPAMP 面板登录用的是**面板登录密钥**，与 CPA 管理密钥是两套东西。排查时先后误改了：

1. `config.yaml` 的 `remote-management.secret-key`（那是 CPA 的管理密钥哈希）
2. `secrets/cpa_management_key`（那是 CPAMP **连 CPA** 用的密钥）
3. 还试过加 `MANAGEMENT_PASSWORD` 环境变量（同样作用于 CPA 管理密钥）

**这三处都不影响面板登录**，所以改了三次都没用。

### 真正的机制（源码级确认）

`apps/manager-server/internal/service/bootstrap/service.go`：

```go
if _, ok, err := st.LoadAdminCredential(ctx); err != nil || ok {
    return false, "", err      // 数据库里已有凭据 → 忽略 secrets 文件
}
adminKey := cfg.AdminKey       // 只有首次启动才读文件
```

面板密钥**只在首次启动时确定**并写入 `/data/usage.sqlite`，此后改任何文件都无效。
唯一改法是 `reset-admin-key` 子命令（`internal/command/adminreset`，直接改写 SQLite）。

### 附带发现

| 发现 | 说明 |
|---|---|
| CPA 有 IP 封禁机制 | 失败 5 次 → 封 30 分钟 → 之后**连正确密钥也返回 403**。排障中触发了两次，因为没先停 CPAMP 就重启 CPA |
| `403` 有三种含义 | `IP banned` / `remote management disabled` / `key not set`；只看状态码分不出，必须看响应体 |
| `secret-key` 要 bcrypt 哈希 | 写明文进去会让认证彻底失效且不可逆。生成哈希时容器若缺 bcrypt，`HASH=$(...)` 会静默把错误信息当哈希 —— 必须校验以 `$2` 开头 |
| 环境变量可绕过哈希 | `MANAGEMENT_PASSWORD` 命中即通过，不走 bcrypt，更适合容器部署（可回退） |

### 对项目的动作

- 教程第八章新增 8.9–8.13（含两张自绘插图：密钥归属图、两条换密钥路径对比）
- README 增加「密钥排障速查」表与三条新现象
- 本次未改动任何代码 —— 排障结论是运维知识，不是代码问题

---

## 四、明确未做的事

| 项 | 原因 |
|---|---|
| 自动检测上游是否需要 TLS 指纹绕过 | 需要实际协议指纹探测能力，超出当前实现范围；现只做字段搬运 |
| `weight` 迁移到 sub2api | sub2api 无对应字段，只能落 notes；负载分配由 `load_factor` 承担 |
| `cloak` 生效 | sub2api 网关层未实现消费逻辑，写入但不生效 |
| 真实 VPS 上的端到端验证 | 需要你在 VPS 上执行；教程给出了分步验证命令 |
| 50 秒级"降级时间"的量化验证 | 无法在本地复现 CF + 多上游的真实链路 |

---

## 五、2026-09-19 实测复查：新发现并已修的问题

本轮以「8 个只读子代理分区勘查全库 + **本机 Docker 实跑端到端**」复核。现场：

- 本机起真实 sub2api（空库，0 账号）与真实 `config.yaml`（162 条目 / 231 个 KEY），
  从**零构建镜像**跑完整导入，逐项核对落库结果；
- 实测确认可跑通的：231 条全部导入成功、每渠道内同域名**同桶**、
  探活计划 **231/231** 挂载成功且 cron 散到 **30 种**不同分钟、
  客户端形态头正确落库（含此前被整段丢弃的 gemini 段）。

### 新发现的 6 个实质缺陷（均非需求文档点名，靠实测暴露）

| # | 缺陷 | 影响 | 状态 |
|---|---|---|---|
| 1 | `GET /accounts/{id}` 的响应带 `{code,data,message}` 信封，代码直接读顶层 | `credentials` / `platform` **恒为 None** → 每条账号都"未找到可用模型" → **231/231 条探活计划全部挂载失败**。而探活是停用账号恢复的唯一证据来源，等于整条自动恢复链路断掉 | ✅ 已修（新增 `_unwrap_one`） |
| 2 | 探活计划只给**本轮新建**的账号挂 | 任何一次挂载失败都会留下**永久缺口**：重跑时这些账号走"已存在→跳过"分支，永远不补挂 | ✅ 已修（已存在账号一并补挂，幂等） |
| 3 | `revive_proven_inactive` 读 `cfg.get(..., False)`，但该键在 DEFAULTS / `设置.json` / `.env.example` **三处都不存在** | 「探活证明可用后自动重新启用」**从未生效**，恒为 False。这正是需求 2⑴「原先关闭的上游检测可用要重新打开」缺的另一半 | ✅ 已修（接线 + 默认 true + 可配） |
| 4 | 智能优先级的归集键不匹配：内部用字符串 `"platform:domain"`，调用方用元组 `(group, host)` | `health_scores.get(k, 0)` 恒取默认值 0 → **生成阶段所有域名同桶**，与健康度完全无关。此前被导入阶段的二次重排掩盖，看不出来 | ✅ 已修（统一键口径 + 账号名反查） |
| 5 | `build_header_overrides` 纯被动搬运，且 `HEADER_OK_PLATFORMS` 不含 gemini | ① 靠 CPA 默认伪装跑通的站点，导入后退化成裸请求 → `503 only allows ...`；② **gemini 段自定义请求头被整段丢弃** | ✅ 已修（补客户端形态头，只补缺不覆盖；gemini 纳入） |
| 6 | `collect` 丢弃空 `api-key` 子条目**不记账** | 用户永远不知道自己漏填了几个 KEY | ✅ 已修（进 `dropped_notes`） |

### 实测暴露的部署链路缺陷（「本机全绿、VPS 报错」的机理）

| # | 缺陷 | 为什么本机看不出来 | 状态 |
|---|---|---|---|
| 7 | 镜像固定 `uid=1000`，bind mount 的 `out/` 属 root 时写不进去 | Windows + Docker Desktop 的 bind mount **权限位不生效**（实测：`chmod` 后仍人人可写） | ✅ 已修（compose 加 `user:`，并在注释里给出按 uid 1000 跑的替代路径） |
| 8 | 挂载缺 `:Z` | 本机不启用 SELinux | ✅ 已修（`:ro,Z` / `:Z`） |
| 9 | 子网硬编码 `172.28.0.0/16` | 开发机通常只跑一两个 stack，网段空着 | ✅ 已修（`CPA2SUB2API_SUBNET` 可覆盖） |
| 10 | 无 `pull_policy`，而 CI 反复覆盖 `:latest` | 本机是 `build` 出来的，天然最新 | ✅ 已修（`always`） |
| 11 | mihomo 的 healthcheck 用 `python3`，**而 metacubex/mihomo 镜像里没有 python3** | 本机没起 mihomo 容器 | ✅ 已修，并**实测**：`HAVE wget/nc/sed/awk/grep`、`MISS curl/python/python3`；新增纯 busybox 的 `healthcheck.sh`，本机实跑验证「代理不通→降级 DIRECT→直连可用→保持健康」全链路 |
| 12 | `mihomo-init` 没有 `env_file`，`MIHOMO_SUBSCRIPTIONS` 的复杂值（含 `=` 和 `;`）传不进容器 | `env_file` 在 `docker compose config` 里不展开，静态看不出 | ✅ 已修（加 `env_file`，并**实测**复杂值能正确传入） |
| 13 | config.yaml 被 Docker 建成**空目录**时，`[ ! -r ]` 拦不住（目录也可读） | 本机 config.yaml 一直存在 | ✅ 已修（文档补齐两种处置方式；实测复现了 `[Errno 21] Is a directory`） |
| 14 | `.dockerignore` 未排除 `设置.json.bak-<时间戳>`、`docx_temp/`、`graphify-out/`、`.upstream_cache/`、子目录 `__pycache__/` | 无 | ✅ 已修 |
| 15 | VPS 上 mihomo 的 healthcheck 只探 9090 API、不判出口 | 节点全挂时进程本身健康（端口在听、API 有响应），容器仍报 healthy，而全部上游请求在代理里排队到超时，表面是上游 502/524 | ✅ 已修：加一级"经 7890 实际出网"判据（**实测**：API 不可达 → exit 1；代理不通、直连可用 → exit 1） |

### 顺带修掉的一致性问题

- `_pick_probe_model` 未传 `source_section`：探活选的模型可能与白名单不同族。
  定时探活会去打一个白名单里根本没放行的模型，那样成功/失败都不能代表账号真实可用性。
- `ping()` 对 `423 Locked` 无提示：见下条。
- **新发现一个很容易误判的机制**：sub2api 对管理员有**合规确认门禁**，
  管理员没在面板确认之前，所有 `/api/v1/admin/*` 返回 `423 Locked`。
  密钥是对的、地址是对的，症状却像认证失败。已在 `ping()` 里翻译成人话，
  并写进教程 11.1 与 README 排障表。

### 本轮没做的事（诚实标注）

| 项 | 原因 |
|---|---|
| 真实 VPS 上的端到端验证 | 只能你在 VPS 上执行；教程第十一章给了逐条验收命令 |
| 自动检测上游是否需要 TLS 指纹绕过 | 仍超出实现范围（需真实协议指纹探测能力）；只做字段搬运 |
| `cloak` 生效 | sub2api 网关层未实现消费逻辑，写入但不生效（已在 README「已知限制」中列明） |
| `weight` 迁移到 sub2api | sub2api 无对应字段，只能落 notes |
| 50 秒级"降级时间"量化 | 无法在本地复现 CF + 多上游的真实链路 |
